import { Prisma, InventoryType } from '@prisma/client'; import { InventoryError, NotFoundError } from '../../core/errors/app-error.js';
export async function changeInventory(tx:Prisma.TransactionClient,input:{variantId:string;change:number;type:InventoryType;reason:string;actorId?:string;orderId?:string;transactionReference?:string}){const inventory=await tx.inventory.findUnique({where:{variantId:input.variantId}});if(!inventory)throw new NotFoundError('Inventory record not found');const next=inventory.quantity+input.change;if(next<0)throw new InventoryError();const updated=await tx.inventory.update({where:{id:inventory.id},data:{quantity:next}});await tx.inventoryTransaction.create({data:{inventoryId:inventory.id,orderId:input.orderId,actorId:input.actorId,type:input.type,previousQuantity:inventory.quantity,quantityChanged:input.change,newQuantity:updated.quantity,reason:input.reason,transactionReference:input.transactionReference}});return updated;}
