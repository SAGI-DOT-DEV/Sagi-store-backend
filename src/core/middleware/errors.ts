import type { ErrorRequestHandler, RequestHandler } from 'express'; import { ZodError } from 'zod'; import { AppError, ValidationError } from '../errors/app-error.js'; import { logger } from '../logger/logger.js';
export const notFound:RequestHandler=(req,_res,next)=>next(new AppError('NOT_FOUND',404,`Route ${req.method} ${req.path} not found`));
export const errorHandler:ErrorRequestHandler=(err,req,res,_next)=>{const error=err instanceof ZodError?new ValidationError('Invalid request',err.flatten()):err instanceof AppError?err:new AppError('INTERNAL_ERROR',500,'An unexpected error occurred'); if(error.statusCode>=500)logger.error({err,requestId:req.requestId},'request failed'); res.status(error.statusCode).json({success:false,error:{code:error.code,message:error.message,requestId:req.requestId, ...(error.details?{details:error.details}:{})}});};
