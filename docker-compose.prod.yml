services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: sagi-backend:latest
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "${HOST_PORT:-3000}:3000"
    init: true
    stop_grace_period: 20s
