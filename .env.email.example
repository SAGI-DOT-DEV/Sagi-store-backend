# Render free: use HTTPS delivery, not Gmail SMTP.
EMAIL_PROVIDER=resend
RESEND_API_KEY=
EMAIL_FROM="SAGI <notifications@YOUR-VERIFIED-DOMAIN>"
EMAIL_TIMEOUT_MS=10000
# Local SMTP remains available with EMAIL_PROVIDER=gmail and existing Gmail settings.
